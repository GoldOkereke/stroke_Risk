# ============================================================
# Dev-only requirements — not installed in production
# Install with: pip install -r requirements-dev.txt
# ============================================================

-r requirements.txt              # includes all production deps

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0                # coverage reports
httpx==0.27.0                    # FastAPI TestClient

# Code quality
black==24.4.2
ruff==0.4.4
mypy==1.10.0                     # type checking
pre-commit==3.7.1

# Notebooks (for model training / EDA)
jupyter==1.0.0
matplotlib==3.9.0
seaborn==0.13.2