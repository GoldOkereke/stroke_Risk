#app settings - these are loaded from environment variables or .env file
#Make sure to create a .env file with the necessary variables (see .env.example for

APP_NAME=Stroke Detection API
APP_VERSION=1.0.0
DEBUG=True
LOG_LEVEL=INFO

#Server settings
PORT=8000

#secret key for JWT tokens (in production, use a strong random value and keep it secret!)
SECRET_KEY=your_secret_key_here